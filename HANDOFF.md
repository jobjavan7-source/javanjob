# HANDOFF — وضعیت کار روی سایت javanjob.ir

> آخرین به‌روزرسانی: ۱۳ سپتامبر ۲۰۲۶
> این فایل برای ادامه‌ی کار توسط نفر/عاملِ بعدی نوشته شده است.
> **هیچ رمز، توکن، کلید API یا اطلاعات ورود در این فایل نیست.** بخش «محل نگهداری اعتبارنامه‌ها» را ببینید.

---

## ✅ به‌روزرسانی ۱۴ سپتامبر ۲۰۲۶ — مشکل حل شد

**علت ریشه‌ای:** افزونه‌ی `resmushit-image-optimizer` (reSmush.it) متد `process_images()` را به
فیلتر `wp_generate_attachment_metadata` وصل می‌کند، و طبق شرط `$doing_cron` در
`classes/Controller/ProcessController.php` این کار را **هر وقت WP-Cron در حال اجراست بدون
قید و شرط** انجام می‌دهد — یعنی دقیقاً در همان مسیری که مرحله‌ی عکس ما اجرا می‌شود. آن متد هر
عکس را به `http://api.resmush.it/` می‌فرستد، در حالی که در `classes/resmushit.class.php` فقط
`CURLOPT_CONNECTTIMEOUT` روی ۱۰ ثانیه تنظیم شده و **`CURLOPT_TIMEOUT` (سقف کل زمان انتقال)
هرگز تنظیم نشده است**. پس اگر اتصال برقرار شود ولی انتقال از سرور ایران متوقف بماند، curl بدون
سقف منتظر می‌ماند تا هاست خودِ پروسه‌ی PHP را بکشد — همان kill‌ای که با `try/catch` گرفته نمی‌شود.

هسته‌ی وردپرس متادیتای پیوست را حین ساخت اندازه‌ها به‌صورت تدریجی ذخیره می‌کند، و به همین دلیل
پیوست با متادیتای کامل و عنوان درست باقی می‌ماند در حالی که `alt_text` و `_thumbnail_id` — که
**بعد از** بازگشت `media_handle_sideload()` نوشته می‌شوند — هرگز اجرا نمی‌شدند.

**نکته‌ی مهم:** خاموش‌کردن گزینه‌ی «بهینه‌سازی هنگام آپلود» این مشکل را حل **نمی‌کند**، چون شرط
`$doing_cron` آن گزینه را دور می‌زند.

**اصلاح:** در `download_and_insert()` فیلتر reSmush.it فقط و فقط برای همان یک آپلود برداشته و
بلافاصله بعد از `media_handle_sideload()` برگردانده می‌شود — همان الگویی که از قبل برای
`intermediate_image_sizes_advanced` وجود داشت. رفتار reSmush.it در بقیه‌ی سایت دست‌نخورده است.

**راستی‌آزمایی (پست ۳۱۶۹، ۲ تصویر):**

| سنجه | قبل (۱۸ تست) | بعد |
|---|---|---|
| `featured_media` | `0` | `3170` ✅ |
| `alt_text` پیوست‌ها | خالی | هر دو پر ✅ |
| عنوان عکاس (`caption`) | خالی | هر دو پر ✅ |
| تصویر درون‌متن | هرگز اجرا نشده بود | یک `<figure>` درج شد ✅ |

**فایل لاگ Apache دیگر لازم نیست** — علت از روی کد و داده‌ی زنده‌ی سایت قطعی شد.

**ابزار دیباگ موقت پاک شد:** `jjaa_debug_marker` از `class-jjaa-generator.php` و
`class-jjaa-settings.php` حذف شد، و پراپرتی بی‌استفاده‌ی `JJAA_Generator::$start_time` هم برداشته شد.
تنها کار باقی‌مانده: حذف آپشن `jjaa_debug_marker` از دیتابیس.

**کد افزونه حالا در ریپو هست** — هر ۸ فایل `jj-ai-articles` زیر
`wp-content/plugins/jj-ai-articles/` نگهداری می‌شود، پس دیگر تنها نسخه‌اش روی سایت زنده نیست.

---

## ۰) صورت مسئله

مقاله‌هایی که افزونه‌ی `jj-ai-articles` با هوش مصنوعی تولید می‌کند دو ایراد دارند:

1. **تصویر شاخص ندارند** (`featured_media` روی پست صفر می‌ماند)
2. **داخل متن مقاله هیچ عکسی نیست**

مورد ۱ هنوز حل نشده. مورد ۲ هرگز فرصت اجرا پیدا نکرده (شاخه‌ی کدش فقط وقتی اجرا می‌شود که بیش از یک عکس موفق شود، و تا امروز هیچ اجرایی به آن نقطه نرسیده).

### قیدهای قطعیِ کارفرما (غیرقابل تغییر)

- **GapGPT هرگز نباید برای تولید مقاله استفاده شود.** فقط هوش مصنوعی واقعاً رایگان (Google Gemini) مجاز است.
- **دامنه‌ی تغییرات فقط «تولید مقاله» است.** پنج کاربرد دیگر GapGPT در سایت (سئو، مدیریت چت، تشخیص توافق در چت، بررسی رزومه با AI، چت‌بات مشاور) **نباید دست بخورند**.
- گزارش‌ها و توضیحات به کارفرما **فقط فارسی** باشد.

---

## ۱) کارهای انجام‌شده

### ۱-۱) زیرساخت — Cloudflare Worker به‌عنوان واسط (relay)

سرور سایت در ایران است و اتصال مستقیم به `generativelanguage.googleapis.com` و `api.pexels.com`
با خطای شبکه (Connection reset) قطع می‌شود. یک Cloudflare Worker به‌عنوان واسط ساخته شد:

- آدرس: `https://broad-wildflower-2bd4.jobjavan7.workers.dev`
- سورس آن در فایل `cloudflare_worker.js` داخل پوشه‌ی scratchpad همین نشست است (باید در جای امنی نگهداری شود؛ داخل ریپو نیست).
- مسیرها:
  | مسیر | مقصد | احراز هویت |
  |---|---|---|
  | `/v1beta/...` | Google Gemini | هدر `X-Relay-Secret` |
  | `/pexels/...` | `api.pexels.com` | هدر `X-Relay-Secret` |
  | `/proxy?url=...&secret=...` | دانلود فایل عکس از دامنه‌ی `pexels.com` | سکرت به‌صورت query param |

مسیر `/proxy` عمداً سکرت را از query param هم می‌پذیرد، چون تابع `download_url()` وردپرس نمی‌تواند هدر سفارشی بفرستد. این مسیر فقط اجازه‌ی دامنه‌ی `pexels.com` را می‌دهد (whitelist).

- سکرت‌های Worker (داخل پنل Cloudflare، نه در کد): `GEMINI_API_KEY`، `RELAY_SECRET`، `PEXELS_API_KEY`.

### ۱-۲) تغییرات کد افزونه `jj-ai-articles`

همه‌ی فایل‌های زیر روی سایت زنده مستقر (deploy) شده‌اند:

| فایل | کاری که شد |
|---|---|
| `includes/class-jjaa-images.php` | بازنویسی کامل مسیر تصاویر (جزئیات پایین‌تر) |
| `includes/class-jjaa-generator.php` | جدا کردن مرحله‌ی پیوست عکس از تولید متن؛ افزودن `run_attach_images()` |
| `includes/class-jjaa-cron.php` | افزودن هوک `jjaa_attach_images` و `schedule_image_attachment()` |
| `includes/class-jjaa-settings.php` | نمایش لاگ آخرین اجرا و تشخیص تصاویر + یک بلوک دیباگ **موقت** |

نسخه‌ی محلیِ این فایل‌ها در پوشه‌ی scratchpad نشست است
(`ai_articles/includes_class-jjaa-*.php`). **این‌ها در ریپو نیستند** — ریپو فعلاً فقط
README و همین فایل را دارد. اگر ادامه‌ی کار لازم شد، باید دوباره از خود سایت
(از طریق ویرایشگر افزونه) خوانده شوند.

جزئیات مهم هر تغییر:

**`class-jjaa-images.php`**
- `search_pexels()` — جست‌وجوی عکس از Pexels از مسیر `/pexels/v1/search` روی Worker.
- `download_and_insert()` — دانلود فایل عکس از مسیر `/proxy` روی Worker، سپس `media_handle_sideload()`.
- فیلتر `intermediate_image_sizes_advanced` فقط برای همین یک آپلود اضافه/حذف می‌شود و اندازه‌ها را
  از ۱۱ تا به ۴ تا محدود می‌کند (`thumbnail`, `medium`, `medium_large`, `large`). سایت ده‌ها اندازه‌ی
  سفارشی دارد (لوگوی کاریابی، پورتفولیو، کاور و…) که پردازش GD آن‌ها روی این هاست بسیار کند بود.
- `generate_and_insert()` — **عمداً به یک stub بدون تماس شبکه تبدیل شده** (دلیلش را در بخش تصمیم‌ها ببینید).
- تنظیم تصویر شاخص با `update_post_meta( $post_id, '_thumbnail_id', $id )` انجام می‌شود، نه
  `set_post_thumbnail()` — دلیلش در بخش تصمیم‌ها.
- کل حلقه داخل `try/catch (\Throwable)` است و بعد از هر عبارت، نتیجه در آپشن
  `jjaa_last_image_diagnostics` ذخیره می‌شود.

**`class-jjaa-generator.php` + `class-jjaa-cron.php`**
- تولید متن مقاله و پیوست‌کردن عکس‌ها حالا **دو درخواست کاملاً جدا** هستند.
  در پایان `create_post()` یک رویداد cron فوری با هوک `jjaa_attach_images` زمان‌بندی و
  `spawn_cron()` صدا زده می‌شود، تا مرحله‌ی عکس بودجه‌ی زمانیِ تازه‌ای از هاست بگیرد.

### ۱-۳) تنظیمات وردپرس (options) که ساخته/پر شده‌اند

| نام آپشن | توضیح |
|---|---|
| `jjaa_relay_url` | آدرس Cloudflare Worker |
| `jjaa_relay_secret` | سکرت واسط |
| `jjaa_gemini_model` | مدل متن (فعلاً `gemini-3.6-flash`) |
| `jjaa_image_gen_model` | مدل تصویرساز (عملاً بلااستفاده — بخش تصمیم‌ها) |
| `jjaa_pexels_api_key` | کلید Pexels |
| `jjaa_last_run_log` | لاگ آخرین اجرای تولید متن |
| `jjaa_last_image_diagnostics` | نتیجه‌ی تفکیکی هر عبارت تصویر |
| `jjaa_debug_marker` | **موقت** — باید حذف شود |
| `jjaa_schedule_migrated_v2` | پرچم مهاجرت زمان‌بندی روزانه → هفتگی |

### ۱-۴) نتایجی که **قطعی و تأییدشده‌اند** (نه حدس)

- ✅ جست‌وجوی Pexels از طریق Worker کار می‌کند.
- ✅ دانلود فایل عکس از طریق `/proxy` کار می‌کند.
- ✅ عکس واقعاً در کتابخانه‌ی رسانه ساخته و به پست وصل می‌شود (`post_parent` درست است) و متادیتای کامل دارد.
- ✅ تولید متن با Gemini از طریق Worker تا انتها کامل می‌شود (بعد از جداسازی async، `jjaa_last_run_log` درست به‌روز شد).
- ✅ تعداد اندازه‌های تولیدی عکس از ۱۱ به ۵ (شامل full) کاهش یافت — روی رسانه‌ی ۳۱۶۲ راستی‌آزمایی شد.

### ۱-۵) چیزهایی که **به‌طور تجربی رد شده‌اند** (وقت خود را برایشان تلف نکنید)

| فرضیه | چرا رد شد |
|---|---|
| کش Cloudflare | پاسخ‌ها `cf-cache-status: DYNAMIC` بودند |
| کهنه بودن OPcache | نشانه‌ی دیباگ بلافاصله به‌روز می‌شد ⇒ کد تازه اجرا می‌شود |
| اجرای موازیِ cron به‌خاطر polling خودم | یک انتظار کاملاً ساکت ۹۰ ثانیه‌ای هم `featured_media:0` داد |
| تمام‌شدن حافظه‌ی PHP | اندازه‌گیری واقعی: ۹۷ مگابایت، اوج ۹۹٫۵ مگابایت — نزدیک سقف نبود |
| خطای قابل‌گرفتن PHP | `try/catch (\Throwable)` هرگز فعال نشد |

---

## ۲) چیزی که نیمه‌کاره مانده و قدم بعدی

### ۲-۱) مشکل باقی‌مانده — دقیقاً چیست

در تست‌های آخر (حدود ۱۸ تست واقعیِ سرتاسری):

- نشانه‌ی دیباگ روی `run_attach_images ENTERED post_id=3167` می‌ماند و **هرگز** به
  `fetch_and_attach RETURNED` نمی‌رسد؛
- ولی رسانه‌ی ۳۱۶۸ **ساخته می‌شود** و به پست ۳۱۶۷ وصل است؛
- `featured_media` صفر می‌ماند و `jjaa_last_image_diagnostics` به‌روز نمی‌شود؛
- `alt_text` و `caption` روی **همه‌ی** پیوست‌های تستی (۳۱۴۴، ۳۱۵۴، ۳۱۵۶، ۳۱۵۸) خالی است.

جمع‌بندی: اجرا دقیقاً **بعد از بازگشت `media_handle_sideload()` و پیش از خط بعدی** قطع می‌شود.
یعنی این دیگر یک باگ منطقی در کد افزونه نیست؛ چیزی در سطح هاست پردازش را می‌کُشد.

شواهد پشتیبان: سقف اجرای حدود ۴۰ ثانیه به‌طور تجربی اثبات شد
(`before media_handle_sideload | elapsed=39.82s | mem=97MB peak=99.5MB`).
`set_time_limit(0)` بی‌اثر است، چون `request_terminate_timeout` در PHP-FPM را نمی‌توان از داخل
PHP بازنویسی کرد و این نوع kill با `try/catch` قابل‌گرفتن نیست.

### ۲-۲) قدم بعدی — دقیقاً همین‌جا متوقف شدیم

**~~منتظر فایل لاگ خطای Apache از کارفرما هستیم.~~ — منتفی شد؛ بخش «به‌روزرسانی» بالای همین فایل را ببینید.**

- مسیر در Plesk: بخش Logs → فایل `logs/error_log` (برچسب «Apache error»، حجم ≈ ۶٫۸۷ مگابایت)
- کارفرما یک‌بار اشتباهاً فایل access log را فرستاد (۴۸۰ کیلوبایت). آن فایل **به درد نمی‌خورد** —
  فقط رکوردهای HTTP دارد و جست‌وجوی `PHP Fatal|terminated|SIGTERM|SIGKILL|max_execution_time|memory exhausted`
  در آن هیچ نتیجه‌ای نداد.

**وقتی فایل رسید:**
- **آن را کامل `Read` نکنید** (از سقف ۲۵۶ کیلوبایت بزرگ‌تر است) — با `grep` بگردید.
- دنبال خطای مرگبار PHP، کشته‌شدن پروسه، یا بلاک‌شدن توسط ModSecurity/WAF بگردید،
  مخصوصاً حول این زمان‌ها (به وقت محلی ایران / وقت سایت):
  - پست ۳۱۶۷ → `2026-09-13 22:46:16`
  - پست ۳۱۶۵ → `2026-09-13 22:40:09`
- اگر لاگ نشان داد که علت `request_terminate_timeout` یا `max_execution_time` است،
  راه‌حل درست **افزایش آن مقدار از طریق Plesk/PHP settings** است، نه تغییر بیشتر در کد.

### ۲-۳) بقیه‌ی کارهای باز

1. **پاک‌سازی ابزار دیباگِ موقت (واجب — هنوز روی سایت زنده است):**
   - `class-jjaa-generator.php` — دو فراخوانی `update_option( 'jjaa_debug_marker', ... )` داخل
     `run_attach_images()` (تقریباً خطوط ۲۴۴ و ۲۵۶)
   - `class-jjaa-settings.php` — خط `$debug_marker = get_option( 'jjaa_debug_marker', '' );` (≈ خط ۹۲)
     و بلوک `<h2>نشانه‌ی دیباگ موقت</h2>` (≈ خطوط ۱۵۷ تا ۱۶۰)
   - در صورت بی‌استفاده بودن: پراپرتی `public static $start_time` در `JJAA_Generator`
   - آپشن `jjaa_debug_marker` از دیتابیس حذف شود
2. **راستی‌آزمایی عکس‌های درون‌متن** — شاخه‌ی `count( $attachment_ids ) > 1` در `run_attach_images()`
   تا امروز **حتی یک‌بار هم اجرا نشده**. بعد از حل مشکل اصلی باید با بیش از یک عکس تست شود.
3. **تصمیم درباره‌ی پست‌های تستی** — باید از کارفرما پرسیده شود که حذف/پیش‌نویس شوند یا بمانند:
   - پست‌ها: ۳۱۳۸، ۳۱۳۹، ۳۱۴۱، ۳۱۴۲، ۳۱۴۳، ۳۱۴۵، ۳۱۴۶، ۳۱۴۹، ۳۱۵۱، ۳۱۵۳، ۳۱۵۵، ۳۱۵۷، ۳۱۵۹، ۳۱۶۱، ۳۱۶۵، ۳۱۶۷
   - رسانه‌ها: ۳۱۴۴، ۳۱۴۷، ۳۱۴۸، ۳۱۵۰، ۳۱۵۲، ۳۱۵۴، ۳۱۵۶، ۳۱۵۸، ۳۱۶۲، ۳۱۶۶، ۳۱۶۸
4. **باگ اجرای تکراری cron** — در تست شماره ۹، یک ارسال فرم سه پست ساخت (۳۱۴۵، ۳۱۴۶، ۳۱۴۹). بررسی نشده.
5. **تسک ۴۸ (نشت توکن در نوار مدیریت برای بررسی رزومه با AI، در افزونه‌ی `javan-job-core`)** —
   مسدود است؛ نیاز به دسترسی به سرویس جداگانه‌ی `ai.javanjob.ir` دارد.

---

## ۳) تصمیم‌های مهم و دلیلشان

| تصمیم | دلیل |
|---|---|
| عبور همه‌ی ترافیک AI و Pexels از Cloudflare Worker | اتصال مستقیم از سرور ایران با Connection reset قطع می‌شود |
| پذیرش سکرت به‌صورت query param فقط در مسیر `/proxy` | `download_url()` وردپرس نمی‌تواند هدر سفارشی بفرستد؛ ریسک با whitelist دامنه‌ی `pexels.com` محدود شد |
| غیرفعال کردن کامل تولید تصویر با Gemini (stub بدون تماس شبکه) | گوگل برای مدل‌های تصویرساز روی سطح رایگان سهمیه‌ی **صفر** می‌دهد (خطای تأییدشده: `quota... limit: 0` برای `generate_content_free_tier`). چون این تماس همیشه شکست می‌خورد و timeout آن تا ۱۲۰ ثانیه بود، انتظار برای یک شکستِ قطعی باعث می‌شد هاست وسط اجرا درخواست را بکشد و حتی تصویر شاخصِ از قبل موفق هم ذخیره نشود. اگر روزی صورتحساب Google Cloud فعال شد، می‌توان تماس واقعی را برگرداند |
| استفاده از `update_post_meta('_thumbnail_id')` به‌جای `set_post_thumbnail()` | `set_post_thumbnail()` برای اعتبارسنجی `wp_get_attachment_image()` را صدا می‌زند که از هوک افزونه‌های دیگر سایت (بهینه‌ساز/CDN عکس) عبور می‌کند و در تست‌های واقعی دقیقاً همان‌جا اجرا متوقف می‌شد. چون خودمان همان لحظه attachment را با موفقیت ساخته‌ایم، آن اعتبارسنجی لازم نیست |
| تنظیم تصویر شاخص بلافاصله بعد از اولین موفقیت (نه در انتهای حلقه) | هاست اجراهای طولانی را می‌کُشد؛ اگر عبارت بعدی کند باشد، تصویرِ از قبل موفق هم از دست می‌رفت |
| ذخیره‌ی diagnostics بعد از **هر** عبارت، نه فقط در انتها | به همان دلیل بالا — که با قطع‌شدن اجرا، اطلاعات تشخیصی از دست نرود |
| محدودکردن اندازه‌های عکس با `intermediate_image_sizes_advanced` | ساخت ۱۱ اندازه (شامل اندازه‌های بی‌ربطِ لوگو/پورتفولیو) با GD روی این هاست ضعیف آن‌قدر طول می‌کشید که درخواست وسط کار قطع می‌شد |
| جدا کردن پیوست عکس به یک درخواست cron مستقل | تا این مرحله بودجه‌ی زمانیِ تازه‌ای از هاست بگیرد. **این تصمیم جواب داد** و مرحله‌ی تولید متن را کاملاً درست کرد |
| زمان‌بندی با `wp_schedule_single_event` زنجیره‌ای به‌جای رویداد تکرارشونده | WP-Cron فقط بازه‌ی ثابت می‌فهمد، نه «روزهای مشخص هفته». برنامه: یکشنبه و پنج‌شنبه ساعت ۱۸:۰۰ به‌وقت تهران |
| راستی‌آزمایی از طریق WP REST API | قابل‌اعتماد و بدون اثر جانبی — برخلاف باز کردن صفحات ادمین که خودش cron را trigger می‌کند |

---

## ۴) تله‌ها و نکاتی که نفر بعدی باید بداند

### استقرار (deploy)

1. **مسیر فایل در اسکریپت استقرار باید مسیر کامل شامل نام پوشه‌ی افزونه باشد.**
   درست: `jj-ai-articles/includes/class-jjaa-images.php`
   غلط: `includes/class-jjaa-images.php` → پاسخ HTTP 500 با پیام
   «ببخشید، این پرونده را نمی‌توان ویرایش کرد.»
2. **ویرایشگر افزونه‌ی وردپرس فقط فایل‌های موجود را ویرایش می‌کند — نمی‌تواند فایل جدید بسازد.**
   هر معماری‌ای که به فایل تازه نیاز دارد با این روش قابل استقرار نیست.
3. اسلاگ منوی افزونه `jjaa-settings` است (نه `jj-ai-articles`).
   آدرس: `/wp-admin/admin.php?page=jjaa-settings`
   استفاده از اسلاگ اشتباه خطای 403 می‌دهد.
4. اجرای دستی تولید مقاله: POST به `admin-post.php` با
   `action=jjaa_create_article`، `_wpnonce`، `jjaa_topic`، `jjaa_word_count`،
   `jjaa_image_count`، `jjaa_publish_mode=now`.
   پاسخ موفق: ریدایرکت 302 به `admin.php?page=jjaa-settings&run=queued`.
   مقدار nonce با هر نشست ورود عوض می‌شود — باید تازه گرفته شود.

### راستی‌آزمایی

5. **برای بررسی نتیجه از WP REST API استفاده کنید، نه از باز کردن صفحه‌ی ادمین:**
   - `/wp-json/wp/v2/posts/{id}?_fields=id,featured_media`
   - `/wp-json/wp/v2/media?parent={id}`
   - `/wp-json/wp/v2/media/{id}?_fields=id,media_details,alt_text`
6. **تله‌ی حلقه‌های انتظار:** چند بار حلقه‌ی `until ... grep -q "<متن موضوع>"` روی صفحه‌ی تنظیمات
   نوشتم که دنبال متنی می‌گشت که اصلاً در آن صفحه ظاهر نمی‌شود — نتیجه: حلقه تا آخرین تکرار
   بی‌فایده می‌چرخید. با REST API و نمونه‌گیری با فاصله‌ی ثابت جایگزین شد.
7. باز کردن مکرر صفحه‌ی ادمین خودش WP-Cron را trigger می‌کند و می‌تواند اجرای تکراری بسازد —
   در تشخیص، این را از باگ واقعی تفکیک کنید.

### سرور

8. سقف اجرای مؤثر حدود **۴۰ ثانیه** است و `set_time_limit(0)` آن را باز نمی‌کند.
   هر مرحله‌ای که ممکن است طولانی شود باید به یک درخواست cron جدا شکسته شود.
9. این kill با `try/catch (\Throwable)` **گرفته نمی‌شود** — نبودِ خطا در لاگ افزونه به‌معنای
   نبودِ مشکل نیست.
10. سایت افزونه‌های زیادی روی هوک‌های پردازش تصویر دارد (بهینه‌ساز عکس/CDN). هر تابعی از وردپرس
    که این هوک‌ها را trigger کند (مثل `wp_get_attachment_image`) می‌تواند نقطه‌ی توقف باشد.

### قواعد کاری کارفرما (دائمی)

1. کار بی‌نتیجه ممنوع
2. آزمون و خطا ممنوع
3. حدس زدن ممنوع
4. هر چیزی را نفهمیدی بپرس
5. هر دسترسی نیاز داری بگو تا داده شود
6. کارها را خودت انجام بده مگر اینکه قوانین اجازه ندهند
7. هر کار را اول به چند مرحله تقسیم کن، بعد مرحله‌به‌مرحله انجام بده
8. فقط وقتی کار کاملاً تمام شد گزارش بده — وسط کار یا در هر مرحله گزارش نده
9. گزارش‌ها مختصر و مفید باشند
10. وقتی کاری را قرار است کارفرما انجام دهد، گام‌به‌گام بگو و تا تأیید یک گام، به گام بعد نرو
11. اطلاعاتی که داده شده (مثل نام کاربری و رمز) را نگه دار و هر بار نپرس

- هر جا کارفرما گفت «قانون جدید»، آن قانون به فهرست بالا اضافه می‌شود.
- همه‌ی گزارش‌ها و توضیحات فقط فارسی باشد.

---

## ۵) محل نگهداری اعتبارنامه‌ها (هیچ‌کدام در این ریپو نیست)

| مورد | محل نگهداری |
|---|---|
| نام کاربری و رمز ادمین وردپرس | نزد کارفرما — در نشست گفت‌وگو ارائه شده |
| کوکی نشست ادمین | فایل موقت `wp_cookies.txt` در پوشه‌ی scratchpad نشست (موقت، پاک‌شدنی) |
| `GEMINI_API_KEY` | متغیر سکرت در پنل Cloudflare Worker |
| `PEXELS_API_KEY` | سکرت Cloudflare Worker + آپشن وردپرس `jjaa_pexels_api_key` |
| `RELAY_SECRET` | سکرت Cloudflare Worker + آپشن وردپرس `jjaa_relay_secret` |
| دسترسی Plesk | نزد کارفرما |

**هیچ‌وقت این مقادیر را در ریپو، در پیام کامیت، یا در توضیحات Pull Request ننویسید.**
