# سامانه‌ی تولید و انتشار خودکار مقاله — `jj-ai-articles`

> آخرین به‌روزرسانی: ۱۴ سپتامبر ۲۰۲۶
> این سند برای نشست/نفر بعدی نوشته شده. هدفش این است که بدون خواندن کل کد،
> بفهمید سامانه چطور کار می‌کند، چه چیزی تغییر کرده، و برای هر تغییری دقیقاً
> کدام فایل و کدام خط را باید دست بزنید.
>
> **هیچ رمز، کلید API یا توکنی در این سند نیست.** بخش «اعتبارنامه‌ها» را ببینید.
>
> سند مکمل: `HANDOFF.md` — تاریخچه‌ی باگ تصویر شاخص و تست‌هایی که برای پیدا
> کردنش انجام شد. آن سند «چرا»ی مفصل را دارد، این سند «چه» و «چطور» را.

---

## ۱) در یک نگاه

افزونه‌ی وردپرسی `jj-ai-articles` در `wp-content/plugins/jj-ai-articles/`
روی سایت javanjob.ir، که به‌صورت خودکار (و در صورت نیاز دستی) مقاله‌ی فارسی
حوزه‌ی «کار و اقتصاد» تولید و منتشر می‌کند: متن با Google Gemini، عکس از Pexels،
سئو و GEO مستقل از افزونه‌های دیگر سایت.

| | |
|---|---|
| صفحه‌ی مدیریت | `/wp-admin/admin.php?page=jjaa-settings` |
| اسلاگ منو | `jjaa-settings` (نه `jj-ai-articles` — اشتباهش ۴۰۳ می‌دهد) |
| موتور متن | Google Gemini، مدل `gemini-3.6-flash` |
| منبع عکس | Pexels |
| زمان‌بندی فعلی | روزانه، ساعت ۱۸:۰۰ به‌وقت تهران |
| کپی کد | همین ریپو، `wp-content/plugins/jj-ai-articles/` |

**قید دائمی کارفرما:** GapGPT هرگز نباید برای تولید مقاله استفاده شود. فقط هوش
مصنوعی رایگان (Gemini). پنج کاربرد دیگر GapGPT در سایت (سئو، مدیریت چت، تشخیص
توافق، بررسی رزومه، چت‌بات) دست‌نخورده‌اند و به این افزونه ربطی ندارند.

---

## ۲) معماری — چرا این‌قدر غیرعادی است

سه محدودیت شکل کل طراحی را تعیین کرده‌اند. اگر این‌ها را ندانید، کد بی‌دلیل
پیچیده به نظر می‌رسد و ممکن است «ساده‌اش کنید» و بشکنید:

### ۲-۱) سرور در ایران است → همه‌چیز از Cloudflare Worker عبور می‌کند

اتصال مستقیم از سرور سایت به `generativelanguage.googleapis.com` و
`api.pexels.com` با Connection reset قطع می‌شود. یک Cloudflare Worker واسط
ساخته شده:

```
https://broad-wildflower-2bd4.jobjavan7.workers.dev
```

| مسیر | مقصد | احراز هویت |
|---|---|---|
| `/v1beta/...` | Google Gemini | هدر `X-Relay-Secret` |
| `/pexels/...` | `api.pexels.com` | هدر `X-Relay-Secret` |
| `/proxy?url=...&secret=...` | دانلود فایل عکس از دامنه‌ی pexels.com | سکرت در query param |

مسیر `/proxy` عمداً سکرت را از query param هم می‌پذیرد، چون تابع
`download_url()` وردپرس نمی‌تواند هدر سفارشی بفرستد. این مسیر فقط دامنه‌ی
`pexels.com` را مجاز می‌داند (whitelist).

سکرت‌های Worker داخل پنل Cloudflare نگهداری می‌شوند: `GEMINI_API_KEY`،
`RELAY_SECRET`، `PEXELS_API_KEY`. کلید Gemini هیچ‌وقت وارد دیتابیس وردپرس نمی‌شود.

### ۲-۲) هاست پردازش را حدود ثانیه‌ی ۴۰ می‌کُشد → تولید به دو درخواست شکسته شده

سقف مؤثر اجرا روی این هاست حدود **۴۰ ثانیه** است و `set_time_limit(0)` بازش
نمی‌کند (چون `request_terminate_timeout` در PHP-FPM را نمی‌شود از داخل PHP
بازنویسی کرد). این نوع kill با `try/catch (\Throwable)` **گرفته نمی‌شود** —
یعنی نبودِ خطا در لاگ به‌معنای نبودِ مشکل نیست.

به همین دلیل:

1. **درخواست اول:** تولید متن مقاله + ساخت پست
2. در پایان آن، یک رویداد cron فوری با هوک `jjaa_attach_images` زمان‌بندی و
   `spawn_cron()` صدا زده می‌شود
3. **درخواست دوم (مستقل، با بودجه‌ی زمانی تازه):** دانلود عکس‌ها، تنظیم تصویر
   شاخص، درج عکس‌های درون‌متن

**این تفکیک را به هم نزنید.** قبل از آن، تولید متن هم ناقص می‌ماند.

### ۲-۳) ویرایشگر افزونه‌ی وردپرس تنها راه استقرار است

دسترسی FTP/SSH/پلسک در نشست‌های قبلی در دسترس نبوده. استقرار فقط از
`/wp-admin/plugin-editor.php` انجام می‌شود، که یعنی:

- **نمی‌توان فایل جدید ساخت.** هر معماری‌ای که به فایل تازه نیاز دارد با این
  روش قابل استقرار نیست.
- **نمی‌توان فایل را تغییر نام داد.** به همین دلیل کلاس `JJAA_Gemini` هنوز در
  فایلی به نام `class-jjaa-gapgpt.php` زندگی می‌کند (بخش ۵-۱).
- مسیر فایل در فرم باید کامل باشد: `jj-ai-articles/includes/class-jjaa-x.php`.
  مسیر ناقص → HTTP 500 با «ببخشید، این پرونده را نمی‌توان ویرایش کرد.»

---

## ۳) فایل‌ها و مسئولیت هرکدام

همه زیر `wp-content/plugins/jj-ai-articles/`:

| فایل | مسئولیت |
|---|---|
| `jj-ai-articles.php` | بوت‌استرپ، require کردن کلاس‌ها، هوک فعال/غیرفعال‌سازی |
| `includes/class-jjaa-gapgpt.php` | **کلاس `JJAA_Gemini`** — کلاینت Gemini از طریق Worker (`chat()`, `list_models()`, `relay_url()`, `relay_secret()`) |
| `includes/class-jjaa-generator.php` | موتور اصلی: ساخت پرامپت، فراخوانی Gemini، پارس JSON، ساخت پست، زمان‌بندی مرحله‌ی عکس |
| `includes/class-jjaa-images.php` | جست‌وجوی Pexels، دانلود از Worker، `media_handle_sideload`، تنظیم تصویر شاخص |
| `includes/class-jjaa-cron.php` | زمان‌بندی خودکار، هوک‌های cron، محاسبه‌ی نوبت بعدی |
| `includes/class-jjaa-settings.php` | صفحه‌ی مدیریت، فرم تولید دستی، فرم زمان‌بندی، تنظیمات API |
| `includes/class-jjaa-seo.php` | متاتگ‌ها، OG/Twitter، اسکیمای Article + FAQPage + BreadcrumbList |
| `includes/class-jjaa-slider.php` | اسلایدر آخرین مقالات |

---

## ۴) جریان کار

### ۴-۱) تولید خودکار

```
رویداد cron «jjaa_scheduled_generate» سررسید می‌شود
  └─ JJ_Cron::run_auto()
       ├─ نوبت بعدی را زمان‌بندی می‌کند   ← اول این، عمداً (بخش ۵-۴)
       └─ JJAA_Generator::run([ word_count, image_count از تنظیمات ])
            ├─ recent_titles(60) → فهرست عنوان‌های اخیر برای پرهیز از تکرار
            ├─ build_prompt()   → پرامپت فارسی + قواعد GEO
            ├─ JJAA_Gemini::chat() → یک JSON با title/meta/body_html/faq/
            │                        image_queries/categories/tags
            ├─ parse_ai_json()  → اعتبارسنجی و پاکسازی
            ├─ create_post()    → درج پست، متاها، برچسب‌ها، دسته‌ها
            └─ schedule_image_attachment() → رویداد cron فوری + spawn_cron()

رویداد «jjaa_attach_images» (درخواست جدا)
  └─ JJAA_Generator::run_attach_images()
       └─ JJAA_Images::fetch_and_attach()
            برای هر عبارت جست‌وجو:
              ├─ search_pexels()      (از مسیر /pexels روی Worker)
              ├─ download_and_insert() (از مسیر /proxy روی Worker)
              │    └─ media_handle_sideload()  ← نقطه‌ی باگ قدیمی، بخش ۵-۳
              └─ اولین عکس موفق → تصویر شاخص (بلافاصله، نه در انتها)
            عکس‌های بعدی → بین بخش‌های H2 پخش می‌شوند
```

### ۴-۲) تولید دستی

فرم بالای صفحه‌ی `jjaa-settings`: موضوع، تعداد کلمه، تعداد عکس، و زمان انتشار
(همین الان / زمان‌بندی برای تاریخ و ساعت مشخص).

- POST به `admin-post.php` با `action=jjaa_create_article`
- همیشه async اجرا می‌شود (هوک `jjaa_manual_generate`) تا روی زنجیره‌ی خودکار
  اثر نگذارد
- پاسخ موفق: ریدایرکت ۳۰۲ به `admin.php?page=jjaa-settings&run=queued`
- مقدار `_wpnonce` با هر نشست عوض می‌شود — باید تازه گرفته شود

---

## ۵) کارهایی که در این دوره انجام شد

هر مورد با هش کامیت روی برنچ `claude/friendly-turing-x4xy8d`.

### ۵-۱) تغییر نام `JJAA_GapGPT` به `JJAA_Gemini` — `7b08b21`

کلاس مدت‌ها بود که با GapGPT کاری نداشت (`chat()` به
`{relay}/v1beta/models/{model}:generateContent` یعنی API گوگل می‌زند) ولی نامش
عوض نشده بود، پس هر کسی کد را می‌خواند نتیجه‌ی غلط می‌گرفت.

- کلاس و همه‌ی نقاط فراخوانی به `JJAA_Gemini` تغییر کرد
- یک `class_alias('JJAA_Gemini', 'JJAA_GapGPT')` برای سازگاری به‌عقب ماند
- **نام فایل هنوز `class-jjaa-gapgpt.php` است** — چون ویرایشگر افزونه فایل
  جدید نمی‌سازد. اگر روزی دسترسی FTP فراهم شد: فایل را به
  `class-jjaa-gemini.php` تغییر نام دهید و خط `require_once` مربوطه را در
  `jj-ai-articles.php` به‌روز کنید.

### ۵-۲) زمان‌بندی و تعداد عکس قابل تنظیم — `5de8864`

قبلاً زمان‌بندی در کد ثابت بود (یکشنبه و پنج‌شنبه ۱۸:۰۰) و `run_auto()` تابع
تولید را **بدون آرگومان** صدا می‌زد، پس عددی که در فرم وارد می‌شد هرگز به
اجرای خودکار نمی‌رسید و همیشه ثابت‌های کد (۳ عکس، ۲۸۰۰ کلمه) استفاده می‌شد.

پنج آپشن جدید:

| آپشن | مقدار فعلی | توضیح |
|---|---|---|
| `jjaa_schedule_mode` | `daily` | `daily` \| `weekly` \| `off` |
| `jjaa_schedule_time` | `18:00` | `HH:MM` به‌وقت سایت |
| `jjaa_schedule_days` | `[0,4]` | ۰=یکشنبه … ۶=شنبه — فقط در حالت `weekly` |
| `jjaa_auto_word_count` | `2800` | ۳۰۰ تا ۶۰۰۰ |
| `jjaa_auto_image_count` | `3` | ۰ تا ۶ (صفر = بدون عکس) |

**تله‌ی مهم:** فرم زمان‌بندی عمداً settings group جدا دارد
(`SCHEDULE_GROUP = 'jjaa_schedule_group'`). دلیلش این است که
`wp-admin/options.php` هنگام ذخیره‌ی یک فرم، روی **همه‌ی** آپشن‌های آن گروه
`update_option` می‌زند و هرکدام که در POST نباشد را `null` می‌کند. اگر هر دو
فرم در یک گروه بودند، ذخیره‌ی زمان‌بندی آدرس Worker و سکرت را پاک می‌کرد و کل
تولید مقاله می‌خوابید. **اگر آپشن جدیدی اضافه می‌کنید، حواستان به گروهش باشد.**

مهاجرت `jjaa_schedule_migrated_v3` یک‌بار پیش‌فرض‌ها را می‌کارد و نوبت را
دوباره می‌سازد، وگرنه رویدادی که از قاعده‌ی هفتگی قبلی در صف مانده بود سر جایش
می‌ماند و تنظیم تازه تا بعد از آن اثر نمی‌کرد.

### ۵-۳) رفع باگ تصویر شاخص — `bb110b3`

مقاله‌ها تصویر شاخص نمی‌گرفتند. عکس ساخته می‌شد، به پست وصل می‌شد، متادیتای
کامل داشت، ولی `featured_media` صفر می‌ماند و `alt_text` خالی.

**علت ریشه‌ای:** افزونه‌ی **reSmush.it** (`resmushit-image-optimizer`) متد
`process_images()` را روی فیلتر `wp_generate_attachment_metadata` می‌نشاند، و
طبق شرط `$doing_cron` در `ProcessController.php` این کار را **هر وقت WP-Cron
اجرا شود بدون قید و شرط** انجام می‌دهد — یعنی دقیقاً مسیر ما. آن متد هر عکس را
به `http://api.resmush.it/` می‌فرستد با `CURLOPT_CONNECTTIMEOUT` برابر ۱۰ ثانیه
ولی **بدون هیچ `CURLOPT_TIMEOUT`** (سقف کل انتقال). پس اگر اتصال برقرار شود و
انتقال از سرور ایران متوقف بماند، curl بی‌نهایت منتظر می‌ماند تا هاست پروسه را
بکشد.

هسته‌ی وردپرس متادیتا را حین ساخت اندازه‌ها تدریجی ذخیره می‌کند — به همین دلیل
پیوست کامل به نظر می‌رسید در حالی که `alt_text` و `_thumbnail_id` که **بعد از**
بازگشت `media_handle_sideload()` نوشته می‌شوند هرگز اجرا نمی‌شدند.

**اصلاح:** در `download_and_insert()` فیلتر reSmush.it فقط برای همان یک آپلود
برداشته و بلافاصله برگردانده می‌شود. رفتارش در بقیه‌ی سایت دست‌نخورده است.

> خاموش‌کردن گزینه‌ی «بهینه‌سازی هنگام آپلود» در reSmush.it این را حل
> **نمی‌کند**، چون شرط `$doing_cron` آن گزینه را دور می‌زند.

**راستی‌آزمایی (پست ۳۱۶۹، ۲ عکس):** `featured_media` از `0` به `3170`،
`alt_text` هر دو پیوست پر، عنوان عکاس پر، و یک `<figure>` درون‌متن — شاخه‌ای که
تا آن روز حتی یک‌بار هم اجرا نشده بود.

### ۵-۴) مقاوم‌سازی زنجیره‌ی زمان‌بندی — `5ddc9de`

WP-Cron «ساعت مشخص در روزهای مشخص» را بلد نیست، پس به‌جای رویداد تکرارشونده،
یک single event زمان‌بندی می‌شود که بعد از هر اجرا نوبت بعدی را می‌سازد
(زنجیره‌ی خودتکرار).

**باگی که پیدا شد:** `run_auto()` اول تولید را انجام می‌داد و **بعد** نوبت بعدی
را زمان‌بندی می‌کرد. اگر تولید شکست می‌خورد یا هاست پروسه را می‌کشت، خط
زمان‌بندی هرگز اجرا نمی‌شد و زنجیره **برای همیشه** می‌مرد. دقیقاً همین در اولین
اجرای روزانه (۱۴ سپتامبر ساعت ۱۸:۰۰) اتفاق افتاد: نه مقاله‌ای ساخته شد، نه
لاگ به‌روز شد، و صف خالی ماند.

**اصلاح:** نوبت بعدی **قبل** از تولید زمان‌بندی می‌شود، به‌علاوه‌ی یک تور ایمنی
— `ensure_scheduled()` در `init()` صدا زده می‌شود، پس اگر نوبت به هر دلیلی از
صف بیفتد، اولین بازدید بعدی دوباره می‌سازدش.

### ۵-۵) موضوع غیرتکراری و تقویت GEO — `fd29809`

- پنجره‌ی عنوان‌های تکراری از ۲۰ به **۶۰** (با تولید روزانه، ۲۰ تا بعد از سه
  هفته دور می‌زد) و پست‌های زمان‌بندی‌شده (`future`) هم حساب می‌شوند
- تعداد `image_queries` درخواستی برابر تعداد عکس واقعی شد (قبلاً همیشه ۳ بود و
  برای عدد بالاتر آخرین عبارت تکرار می‌شد → عکس‌های شبیه هم)
- قواعد GEO به بلوک مستقل تبدیل شد: پاراگراف پاسخ مستقیم ۴۰–۶۰ کلمه‌ای بعد از
  مقدمه، ممنوعیت ارجاع‌هایی مثل «همان‌طور که بالاتر گفتیم» (چون موتورهای
  پاسخ‌گو پاراگراف را جدا نقل می‌کنند و بی‌معنا می‌شود)، و فشار به سمت جزئیات
  عددی

### ۵-۶) پاک‌سازی — `9545e5b`

ابزار دیباگ موقت (`jjaa_debug_marker`) و پراپرتی بی‌استفاده‌ی
`JJAA_Generator::$start_time` حذف شدند.

---

## ۶) اگر بخواهید چیزی را تغییر دهید

| می‌خواهید… | کجا |
|---|---|
| ساعت یا تناوب انتشار | از پنل: `jjaa-settings` → «زمان‌بندی تولید خودکار». نیازی به کد نیست |
| تعداد کلمه/عکس اجرای خودکار | همان فرم |
| **لحن، ساختار، یا قواعد سئو/GEO مقاله** | `class-jjaa-generator.php` → `build_prompt()` — کل پرامپت یک heredoc فارسی است |
| ساختار JSON خروجی مدل | همان `build_prompt()` (بخش پایانی) + `parse_ai_json()` که باید هم‌زمان به‌روز شود |
| مدل Gemini | پنل، فیلد «مدل متن». پیش‌فرض کد: `gemini-flash-latest` |
| پنجره‌ی پرهیز از تکرار موضوع | `class-jjaa-generator.php` → `self::recent_titles( 60 )` |
| منبع یا معیار انتخاب عکس | `class-jjaa-images.php` → `search_pexels()` |
| اندازه‌های تولیدی عکس | `class-jjaa-images.php` → فیلتر `intermediate_image_sizes_advanced` (عمداً به ۴ اندازه محدود شده، چون GD روی این هاست کند است) |
| متاتگ‌ها و اسکیما | `class-jjaa-seo.php` |
| منطق نوبت بعدی | `class-jjaa-cron.php` → `next_occurrence()` |

### تله‌هایی که وقت شما را می‌گیرند اگر ندانید

1. **فرم زمان‌بندی settings group جدا دارد** — بخش ۵-۲ را بخوانید قبل از افزودن
   آپشن جدید
2. **ویرایشگر افزونه فایل جدید نمی‌سازد** — معماری‌ای که به فایل تازه نیاز دارد
   با این مسیر قابل استقرار نیست
3. **اسلاگ منو `jjaa-settings` است**، نه `jj-ai-articles`
4. **WP-Cron ساعت واقعی ندارد** — فقط با بازدید از سایت بررسی می‌شود. روی سایت
   کم‌ترافیک «۱۸:۰۰» عملاً می‌تواند دیرتر شود. راه دقیق‌تر: cron واقعی سرور از
   پلسک + `DISABLE_WP_CRON`
5. **هر مرحله‌ای که ممکن است طولانی شود باید به یک درخواست cron جدا شکسته شود**
   (بخش ۲-۲)
6. **باز کردن مکرر صفحه‌ی ادمین خودش WP-Cron را trigger می‌کند** — در تشخیص،
   این را از باگ واقعی تفکیک کنید

---

## ۷) راستی‌آزمایی — بدون باز کردن پنل

برای بررسی نتیجه از REST API استفاده کنید، نه از باز کردن صفحه‌ی ادمین (که
خودش cron را trigger می‌کند و نتیجه را آلوده می‌کند):

```bash
# آخرین پست‌ها و تصویر شاخصشان
curl -s "https://javanjob.ir/wp-json/wp/v2/posts?per_page=5&_fields=id,date,featured_media,title"

# پیوست‌های یک پست (alt_text باید پر باشد)
curl -s "https://javanjob.ir/wp-json/wp/v2/media?parent=POST_ID&_fields=id,alt_text,caption,media_details"

# یک رسانه‌ی مشخص
curl -s "https://javanjob.ir/wp-json/wp/v2/media/MEDIA_ID?_fields=id,alt_text,media_details"
```

علائم سلامت یک اجرای موفق:
- `featured_media` مخالف صفر
- `alt_text` پیوست‌ها **پر** (اگر خالی است یعنی اجرا بعد از
  `media_handle_sideload` مرده — بخش ۵-۳)
- در صفحه‌ی `jjaa-settings`: «آخرین رویداد … وضعیت: success» و «اجرای بعدی: …»
- اگر «هنوز نوبتی زمان‌بندی نشده» دیدید یعنی زنجیره قطع شده (بخش ۵-۴)

---

## ۸) وضعیت فعلی و کارهای باز

**وضعیت (۱۴ سپتامبر ۲۰۲۶، ساعت ۲۲:۰۰ تهران):**

- تولید متن، تصویر شاخص و تصویر درون‌متن: **کار می‌کنند** (راستی‌آزمایی‌شده روی پست ۳۱۶۹)
- زمان‌بندی: روزانه ۱۸:۰۰ — ولی **زنجیره در اولین اجرا قطع شد** (بخش ۵-۴)
- دو مقاله منتشر شده: ۳۱۳۸ و ۳۱۶۹، هر دو با تصویر شاخص

**کارهای باز:**

1. **اصلاح بخش ۵-۴ هنوز مستقر نشده** — فایل `includes/class-jjaa-cron.php`
   باید روی سایت زده شود. تا آن موقع تولید خودکار متوقف است.
2. **اولین اجرای خودکار موفق هنوز دیده نشده.** اجرای ۱۴ سپتامبر شکست خورد.
   بعد از استقرار اصلاح، نوبت بعدی را رصد کنید.
3. **چرا اجرای ۱۸:۰۰ شکست خورد هنوز ریشه‌یابی نشده** — لاگ به‌روز نشد، یعنی
   `run()` قبل از مرحله‌ی لاگ مرده. محتمل‌ترین گمانه همان دیوار ۴۰ ثانیه است،
   ولی **تأیید نشده**. با تور ایمنیِ جدید، حتی اگر تکرار شود زنجیره نمی‌میرد.
4. **پاک‌سازی از دوره‌ی قبل:** ۱۲ عکس تستی، متن جایگزین رسانه‌ی ۳۱۵۸، خالی‌کردن
   زباله‌دان ۱۷ پست، و حذف آپشن `jjaa_debug_marker` از دیتابیس
   (`DELETE FROM wp_options WHERE option_name='jjaa_debug_marker';` — پیشوند
   جدول را اول تأیید کنید)

---

## ۹) روش استقرار

با توجه به بخش ۲-۳، تنها مسیر ویرایشگر افزونه است:

```
https://javanjob.ir/wp-admin/plugin-editor.php?plugin=jj-ai-articles%2Fjj-ai-articles.php&file=jj-ai-articles%2Fincludes%2FFILENAME
```

برای هر فایل: کلیک در کادر کد → `Ctrl+A` → `Delete` → محتوای فایل را paste →
«به‌روزرسانی پرونده».

- اگر خطای نحوی باشد وردپرس خودش تغییر را برمی‌گرداند
- **همیشه قبل از استقرار `php -l` بزنید**
- اگر چند فایل به هم وابسته‌اند، فایلی که بقیه به آن ارجاع می‌دهند را اول بزنید
- بعد از استقرار، فایل را دوباره از همان صفحه بخوانید و با نسخه‌ی محلی diff
  بگیرید (ویرایشگر یک خط خالی انتهایی اضافه می‌کند — این تفاوت طبیعی است)

---

## ۱۰) اعتبارنامه‌ها — هیچ‌کدام در این ریپو نیست

| مورد | محل نگهداری |
|---|---|
| ادمین وردپرس | نزد کارفرما |
| دسترسی پلسک | نزد کارفرما (پورت ۸۴۴۳) |
| `GEMINI_API_KEY` | سکرت Cloudflare Worker |
| `PEXELS_API_KEY` | سکرت Worker + آپشن `jjaa_pexels_api_key` |
| `RELAY_SECRET` | سکرت Worker + آپشن `jjaa_relay_secret` |

**هیچ‌وقت این مقادیر را در ریپو، پیام کامیت یا توضیحات PR ننویسید.**

---

## ۱۱) قواعد کاری کارفرما (دائمی)

۱. کار بی‌نتیجه ممنوع
۲. آزمون و خطا ممنوع
۳. حدس زدن ممنوع
۴. هر چیزی را نفهمیدی بپرس
۵. هر دسترسی نیاز داری بگو تا داده شود
۶. کارها را خودت انجام بده مگر اینکه قوانین اجازه ندهند
۷. هر کار را اول به چند مرحله تقسیم کن، بعد مرحله‌به‌مرحله انجام بده
۸. فقط وقتی کار کاملاً تمام شد گزارش بده — وسط کار گزارش نده
۹. گزارش‌ها مختصر و مفید باشند
۱۰. وقتی کاری را کارفرما باید انجام دهد، گام‌به‌گام بگو و تا تأیید یک گام به
    گام بعد نرو
۱۱. اطلاعات داده‌شده (نام کاربری، رمز) را نگه دار و هر بار نپرس

هر جا کارفرما گفت «قانون جدید»، آن قانون به این فهرست اضافه می‌شود.
همه‌ی گزارش‌ها و توضیحات فقط فارسی.
